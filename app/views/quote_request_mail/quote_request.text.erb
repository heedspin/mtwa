
-------- Quote Request -------------
From:  <%= @quote_request.name_email %>
To:    LXD
Sent:  <%= @quote_request.created_at %>

<% (QuoteRequest.fields - [:name, :email, :created_at, :note]).each do |field| %>
	<% if (value = @quote_request.send(field)).present? %>
	<%= QuoteRequest.human_attribute_name(field)%>:
		<% if value.is_a?(Array) %>
		<%= raw value.join(', ') %>
		<% else %>
		<%= value %>
		<% end %>
	<% end %>
<% end %>

<%= @quote_request.note %>