<table class="mtdata">
  <thead>
    <tr>
			<% (0..columns-1).each do |column| %>
			<th><%= table_input(object, field, 0, column, 14) %></th>
			<% end %>
		</tr>
	</thead>
  <tbody>
		<% (1..rows).each do |row| %>
    <tr class="<%= cycle('even', 'odd') %>">
			<% (0..columns-1).each do |column| %>
			<td><%= table_input(object, field, row, column) %></td>
		<% end %>
		</tr>
		<% end %>
	</tbody>
</table>
